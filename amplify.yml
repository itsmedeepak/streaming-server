version: 1
applications:
  - appRoot: .
    backend:
      phases:
        preBuild:
          commands:
            - npm install
      artifacts:
        baseDirectory: . # The directory where your server files are located
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
